fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
numpy>=1.26
scikit-learn>=1.4
networkx>=3.2
python-dateutil>=2.9

# Lets pydantic resolve `X | Y` union annotations on Python < 3.10.
eval_type_backport>=0.2; python_version < "3.10"

# --- Optional upgrades (heavier; not required to run) ---
# Enable semantic embeddings instead of the TF-IDF fallback:
#   pip install sentence-transformers
#   EMBED_BACKEND=minilm
# Better topic labels:
#   pip install bertopic
